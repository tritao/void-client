# Symbol rename mappings (methods/fields/params/locals) applied via JDTLS/LSP.
#
# This file is intended to be:
# - data-driven (append rows)
# - idempotent (reruns skip already-applied renames)
# - symbol-aware (JDTLS updates callsites safely)
#
# Columns (recommended long-term):
# - file: path to a .java file (relative to repo root).
#   If it has no '/', it is treated as either:
#   - a stem under client/src/ (e.g. Class348_Sub49 or Class348_Sub49.java), OR
#   - a *renamed* type from client/refactor/classes.csv (e.g. JagBuffer or JagBuffer.java).
# - kind: type | method | field | param | local
# - owner: optional owning type name (recommended for method/field/param).
#   Like `file`, `owner` may be either the obfuscated type name (client/src) or the
#   refactored type name (classes.csv destination); tools may translate as needed.
# - member: optional owning member name (required for param; e.g. the method name).
# - signature: optional method signature used to disambiguate overloads (e.g. "(int,String)").
# - param_index: optional 0-based parameter index (required for kind=param unless using line/col).
# - old: existing identifier (recommended; for idempotence we also treat "already renamed" as ok)
# - new: desired identifier
# - detail_regex: optional, regex to disambiguate overloaded methods via LSP "detail" (method/field/type)
# - line: optional 1-based line to rename at (escape hatch; required for locals for now)
# - col: optional 1-based column to rename at
# - notes: optional free-form text (ignored by the tool)
#
# Notes:
# - For kind=param: prefer owner+member+signature+param_index over line/col.
# - For kind=local: line/col is currently required.
# - For kind=method/field/type: prefer owner + (signature or detail_regex when needed) over line/col.
#
# Example (method, located via documentSymbol):
# client/src/SomeClass.java,method,SomeClass,,,(int,String),,method1234,decodeSomething,,,
#
# Example (param, signature-based; no line numbers):
# client/src/SomeClass.java,param,SomeClass,decodeSomething,(int,String),1,i,itemId,,,
#
# Example (local, explicit position):
# client/src/SomeClass.java,local,,,,,,i,itemId,,123,17,"rename local i -> itemId"
#
file,kind,owner,member,signature,param_index,old,new,detail_regex,line,col,notes
